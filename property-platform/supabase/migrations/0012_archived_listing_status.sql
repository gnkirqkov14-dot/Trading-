-- Property Platform — 3-степенна схема за напомняния (виж 0013 за детайли).
--
-- Отделна миграция само за новата enum стойност: Postgres не позволява
-- нова enum стойност да се ползва в СЪЩАТА транзакция, в която е
-- добавена, затова функцията, която я ползва, е в отделен файл (0013),
-- пуснат СЛЕД тази миграция.

alter type public.listing_status add value 'archived';
